{
  "name": "monroy-qms",
  "private": true,
  "workspaces": ["api", "public"],
  "scripts": {
    "install:all": "npm --prefix api install && npm --prefix public install",
    "build:web": "npm --prefix public run build",
    "build:api": "npm --prefix api run prisma:generate",
    "copy:web": "node scripts/copy-web-dist.js",
    "build": "npm run install:all && npm run build:web && npm run build:api && npm run copy:web",
    "start": "npm --prefix api run start:prod",
    "seed": "npm --prefix api run seed"
  }
}
